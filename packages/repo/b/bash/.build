tar -xf $filename
pckname=${filename%.t*}
cd bash-5.1.16 || cd $pckname

./configure --prefix=/usr                      \
            --docdir=/usr/share/doc/bash-5.1.16 \
            --without-bash-malloc              \
            --with-installed-readline

make
make DESTDIR=$pckdir install

